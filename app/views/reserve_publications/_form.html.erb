<%= show_error_message @reservation %>

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @reservation.publication.name %>
</p>

<p>
  <strong>Author:</strong>
  <%= @reservation.publication.author %>
</p>

<p>
  <strong>Isbn:</strong>
  <%= @reservation.publication.try(:isbn) %>
</p>

<p>
  <strong>Issn:</strong>
  <%= @reservation.publication.try(:issn) %>
</p>

<p>
  <strong>Age limit:</strong>
  <%= @reservation.publication.age_limit %>
</p>

<p>
  <strong>Status:</strong>
  <%= @reservation.publication.status %>
</p>

<% if current_user %>
  <% if @reservation.publication.available? %>
    <%= link_to 'Borrowing', borrowings_path(borrowing: { "#{@reservation.publication.class.name.downcase}_id" => @reservation.publication.id }), method: :post %>
  <% end %>
  <%= link_to 'Reservation', reservations_path(reservation: { "#{@reservation.publication.class.name.downcase}_id" => @reservation.publication.id }), method: :post %>
<% end %>
